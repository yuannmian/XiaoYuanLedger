# 小圆饼干 v10

本版本修复自定义弹窗底部“取消 / 保存”按钮被裁切的问题。

## 修复内容
- 记录支出弹窗：底部按钮完整显示。
- 记录收入弹窗：底部按钮完整显示。
- 调整总存款弹窗：底部按钮完整显示。
- 移除会造成裁切的按钮顶部边距与错误固定高度组合。
- 按钮区域改为独立顶部间距，并保留 24dp 底部安全留白。

上传全部文件到原 GitHub 仓库并覆盖同名文件，随后运行 Actions 重新构建 APK。

## v11 更新
- 已设置总存款且金额不为 0 时，冷启动应用会自动打开“记录支出”。
- 自动聚焦“多少钱”输入框并弹出数字键盘。
- 旋转屏幕、切换皮肤重建页面时不会重复自动弹窗。
